#Anthony Palomera
#CS176A Hw3
Partner:Mauricio Munoz

#UDP Server and Client
    #Socket is created
    #Data is sent to socket
    #Receives Response
    #Checks for only numeric values+ adds digits up until it is single
    #Make sure to put a sleep method to help packets arrive at proper time
    #Closes socket



#TCP Server and Client
    #Socket is created
    #Data is sent to socket
    #Client asks to connect to Server
    #Receives Response
    #Checks for only numeric values+ adds digits up until it is single
    #Closes socket
